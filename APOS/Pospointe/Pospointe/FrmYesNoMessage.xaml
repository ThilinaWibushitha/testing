<Window x:Class="Pospointe.FrmYesNoMessage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ResizeMode="NoResize"
        WindowState="Maximized"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        Title="FrmYesNoMessage" Width="1920" Height="1080">

    <Grid>
        <Border Background="#80000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Border.Effect>
                <BlurEffect Radius="20" />
            </Border.Effect>
        </Border>

        <Border Padding="20" CornerRadius="25"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="{StaticResource BackgroundBrush}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="Black" Opacity="0.5" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource Appboarderfill}" Padding="15" CornerRadius="15" Margin="15">
                    <TextBlock x:Name="LblHeader" Text="Confirmation"
                               HorizontalAlignment="Center"
                               FontSize="24" FontWeight="Bold" 
                               Style="{StaticResource Header1}" />
                </Border>

                <TextBlock x:Name="LblMessage" Grid.Row="1"
                           Text="Are you sure?"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           FontSize="20"
                           Style="{StaticResource Header2}"
                           Width="600" Margin="15"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="15">
                    <Button Content="Yes" Click="BtnYes_Click"
                            Width="180" Height="50" FontSize="16" FontWeight="Bold"
                            BorderThickness="0" Style="{StaticResource btnToolbox}" Margin="10"/>

                    <Button Content="No" Click="BtnNo_Click"
                            Width="180" Height="50" FontSize="16" FontWeight="Bold"
                            BorderThickness="0" Style="{StaticResource btnToolbox}" Margin="10"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
