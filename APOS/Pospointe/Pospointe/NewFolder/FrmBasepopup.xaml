<Window x:Class="Pospointe.NewFolder.FrmBasepopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pospointe.NewFolder"
        mc:Ignorable="d"
        Title="FrmBasepopup" Height="450" Width="800">
    <Grid Background="#80000000">
        <!-- Semi-transparent dark background -->
        <Border Background="White" CornerRadius="20" Padding="15" Width="280"
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <!-- Title -->
                <TextBlock Text="This is a confirm dialog" 
                           FontSize="16" FontWeight="Bold"
                           TextAlignment="Center" Margin="0,5,0,0"/>

                <!-- Message -->
                <TextBlock Text="With Custom Confirm Message." 
                           FontSize="14" Foreground="Gray"
                           TextAlignment="Center" Margin="0,5,0,15"/>

                <!-- Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Confirm Button -->
                    <Button Content="Confirm" Width="120" Height="40"
                            Background="White" Foreground="DodgerBlue"
                            BorderThickness="0" FontSize="14"
                            Grid.Column="0" />

                    <!-- Cancel Button -->
                    <Button Content="Cancel" Width="120" Height="40"
                            Background="White" Foreground="DodgerBlue"
                            FontWeight="Bold" BorderThickness="0"
                            FontSize="14" Grid.Column="1"
                            />
                </Grid>

            </StackPanel>
        </Border>
    </Grid>
</Window>
